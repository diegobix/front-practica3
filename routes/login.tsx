import MainContainer from "../components/MainContainer.tsx";
import LoginForm from "../islands/LoginForm.tsx";

export default () => {
  return (
    <MainContainer>
      <LoginForm />
    </MainContainer>
  );
};
